# Next.js 14 declares peer dependency on React 18, but this project
# uses React 19. This flag allows npm install to proceed without
# failing on the version mismatch. Remove once Next.js is upgraded
# to a version that supports React 19 natively.
legacy-peer-deps=true
